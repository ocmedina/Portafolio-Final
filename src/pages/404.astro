---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="404 - Página no encontrada | Octavio Medina"
  description="La página que buscas no existe o ha sido movida."
>
  <section class="min-h-screen flex items-center justify-center px-6 py-20">
    <div class="max-w-2xl text-center">
      <!-- Número 404 animado -->
      <div class="mb-8 relative">
        <h1
          class="text-[150px] md:text-[200px] font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-500 to-purple-600 leading-none animate-pulse-slow"
        >
          404
        </h1>
        <div class="absolute inset-0 flex items-center justify-center">
          <div class="text-6xl animate-bounce-slow">🔍</div>
        </div>
      </div>

      <!-- Mensaje -->
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
        ¡Ups! Página no encontrada
      </h2>
      <p class="text-slate-400 text-lg mb-8 max-w-md mx-auto">
        Parece que la página que buscas no existe, fue movida o el enlace está
        roto.
      </p>

      <!-- Sugerencias -->
      <div
        class="bg-slate-900/50 backdrop-blur-sm border border-slate-800 rounded-xl p-6 mb-8 text-left"
      >
        <h3 class="text-xl font-semibold text-blue-400 mb-4">
          ¿Qué puedes hacer?
        </h3>
        <ul class="space-y-3 text-slate-300">
          <li class="flex items-start gap-3">
            <span class="text-blue-400 mt-1">✓</span>
            <span>Verifica que la URL esté escrita correctamente</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-blue-400 mt-1">✓</span>
            <span>Regresa a la página de inicio y navega desde allí</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-blue-400 mt-1">✓</span>
            <span>Usa el menú de navegación para encontrar lo que buscas</span>
          </li>
        </ul>
      </div>

      <!-- Botones de navegación -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/"
          class="px-8 py-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-blue-500/50 flex items-center justify-center gap-2"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
            ></path>
          </svg>
          Ir al Inicio
        </a>

        <a
          href="/#proyectos"
          class="px-8 py-4 bg-slate-800 hover:bg-slate-700 text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 border border-slate-700 hover:border-blue-500 flex items-center justify-center gap-2"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
            ></path>
          </svg>
          Ver Proyectos
        </a>

        <a
          href="/#contacto"
          class="px-8 py-4 bg-slate-800 hover:bg-slate-700 text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 border border-slate-700 hover:border-purple-500 flex items-center justify-center gap-2"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
            ></path>
          </svg>
          Contacto
        </a>
      </div>

      <!-- Mensaje divertido -->
      <p class="mt-12 text-slate-500 text-sm italic">
        "No todos los que vagan están perdidos... pero esta página sí lo está
        😅"
      </p>
    </div>
  </section>
</Layout>

<style>
  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.7;
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 3s ease-in-out infinite;
  }

  @keyframes bounce-slow {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }
</style>
